enum API_Decision { NONE, Only_Success, Only_Failure, BOTH }

class API_BAKCHODI {
  static int patani = 0;
}

/*
API_BAKCHODI.patani == 0;
*/

/**
 * 
 * API_Decision showStatus
 * eventNameSaveCall (API_Decision.Only_Failure)
 * 
 * 
 * if (showStatus == API_DECISION.Only_Success || showStatus == API_DECISION.BOTH) {
 * show error
 * } else {
 *  show error
 * }
 * 
 */